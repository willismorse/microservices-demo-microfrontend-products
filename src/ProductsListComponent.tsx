import * as React from 'react';

export interface ProductsListComponentProps {
}

export interface ProductsListComponentState {
}

export default class ProductsListComponent extends React.Component<ProductsListComponentProps, ProductsListComponentState> {
  constructor(props: ProductsListComponentProps) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <div>
        asd
      </div>
    );
  }
}

